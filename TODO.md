# TODO / 改善バックログ

2026-07-12 のコードレビューで洗い出した残課題。

## ほぼバグ（優先度高）

- [x] **現在地マーカー未表示** — `Map.tsx` に現在地マーカー（青点＋精度円）を追加済み（2026-07-13）。`currentLocationAtom` は `{ position, accuracy } | null` に変更し、未取得時は非表示。
- [x] **位置情報取得失敗が無言** — 権限拒否・タイムアウト時に Snackbar（上部中央、warning）で通知するよう対応済み（2026-07-13）。通知はボタン手動クリック時のみで、初回ロードの自動取得失敗では出さない。
- [x] **ポップアップが狭い** — `maxWidth` を 200px に拡大済み（2026-07-13）。

## 使いやすさ

- [x] **店名検索** — 対応済み（2026-07-13）。右上の虫めがねボタン→ダイアログで全カテゴリ横断検索（カナ/かな・全半角を正規化、前方一致優先）。選択で該当カテゴリをONにして flyTo＋ポップアップ自動表示。全カテゴリのデータは検索初回オープン時に遅延ロード。
- [x] **地図状態のURL共有・復元** — 対応済み（2026-07-13）。OSM風ハッシュ `#map=<zoom>/<lat>/<lng>&cats=ramen,udon` を moveend/カテゴリ変更で `history.replaceState` 更新。復元優先度はハッシュ > localStorage > デフォルト。ハッシュ付きで開いた場合は初回の現在地自動取得で地図を動かさない（`utils/mapHash.ts`）。
- [x] **周辺リスト表示** — 対応済み（2026-07-13）。`ShopListSheet` に「📍 周辺」タブを追加（デフォルトタブ）。表示中カテゴリの店を現在地（なければ地図中心=`mapViewAtom`）から近い順に30件、距離表示付き。
- [x] **マーカーのカテゴリ識別** — 対応済み（2026-07-13）。枠線色を `switchColor` に変更（チップと対応）。行きたい/行ったマークはバッジのみで表現。divIcon は（カテゴリ×マーク）でメモ化済み。
- [x] **PWA対応** — 対応済み（2026-07-13）。`public/manifest.json` + `_app.tsx` に manifest リンクと theme-color を追加（Service Worker なしの最小構成、アイコンは既存の icon-192/512 を利用）。

## 追加コンテンツ

- [x] **行きたい/行ったマーク** — 対応済み（2026-07-13）。ポップアップ内のチップでトグルし `atomWithStorage` で永続化（キーは tabelog URL。`id` は index 連番でデータ更新時にずれるため不使用）。マーカーに色付きバッジ（⭐/✓）表示、カテゴリシートにカテゴリ別達成数表示。
- [x] **行きたい/行った店の一覧** — 対応済み（2026-07-13）。右上の星ボタン→タブ式シート（`ShopListSheet`）。行タップで該当店へ地図移動、ゴミ箱でマーク解除。データ更新で百名店から外れた店はグレー表示＋食べログリンク（マーク保存時に店名も保持するようにした）。
- [x] **受賞年の表示** — 対応済み（2026-07-13）。`code` は build-data で保持されていたため、ポップアップで年をパースしてバッジ表示（`utils/shops.ts` の `getAwardYear`）。全7078店で年の取得を確認済み（2022〜2026、カテゴリにより最新版の年が異なる）。
- ~~データ更新日の表示~~ — 見送り（2026-07-13）。最新の百名店データしか表示しない運用のため、店ごとの受賞年バッジで十分と判断。

## 推奨着手順

~~現在地マーカー → 店名検索 → 行った/行きたいマーク~~（すべて対応済み）

## 実装メモ

- `ShopMarker` の `position` は `useMemo` で参照を安定させること。毎レンダー新しい配列を渡すと react-leaflet が `setLatLng` を呼び、markercluster がマーカーを付け外しして開いているポップアップが閉じる。
