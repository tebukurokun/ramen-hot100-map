# TODO / 改善バックログ

2026-07-12 のコードレビューで洗い出した残課題。

## ほぼバグ（優先度高）

- [x] **現在地マーカー未表示** — `Map.tsx` に現在地マーカー（青点＋精度円）を追加済み（2026-07-13）。`currentLocationAtom` は `{ position, accuracy } | null` に変更し、未取得時は非表示。
- [x] **位置情報取得失敗が無言** — 権限拒否・タイムアウト時に Snackbar（上部中央、warning）で通知するよう対応済み（2026-07-13）。通知はボタン手動クリック時のみで、初回ロードの自動取得失敗では出さない。
- [x] **ポップアップが狭い** — `maxWidth` を 200px に拡大済み（2026-07-13）。

## 使いやすさ

- [x] **店名検索** — 対応済み（2026-07-13）。右上の虫めがねボタン→ダイアログで全カテゴリ横断検索（カナ/かな・全半角を正規化、前方一致優先）。選択で該当カテゴリをONにして flyTo＋ポップアップ自動表示。全カテゴリのデータは検索初回オープン時に遅延ロード。
- [ ] **地図状態のURL共有・復元** — 中心座標・ズーム・ONカテゴリをURLハッシュに載せて共有/リロード復元できるようにする。あわせて `markerVisibilityAtom` の localStorage 永続化も検討。
- [ ] **周辺リスト表示** — 表示中カテゴリの店を現在地/viewport から距離順に並べるリスト（ボトムシートにタブ追加など）。
- [ ] **マーカーのカテゴリ識別** — `createEmojiIcon` の枠線色（固定 `#666`）を `switchColor` にしてチップと色を対応させる。divIcon をカテゴリ単位でメモ化すればレンダリングも軽くなる。
- [ ] **PWA対応** — manifest + アイコン追加でホーム画面インストール可能にする（Service Worker なしの最小構成でOK）。

## 追加コンテンツ

- [x] **行きたい/行ったマーク** — 対応済み（2026-07-13）。ポップアップ内のチップでトグルし `atomWithStorage` で永続化（キーは tabelog URL。`id` は index 連番でデータ更新時にずれるため不使用）。マーカーに色付きバッジ（⭐/✓）表示、カテゴリシートにカテゴリ別達成数表示。
- [ ] **受賞年の表示** — JSON の `code` に年が入っている（例 `hyakumeiten_ramen_aichi_2025_...`）が build-data で捨てられている。ポップアップに受賞年バッジを出す。
- [ ] **データ更新日の表示** — 地図UIに「データ: YYYY-MM-DD 時点」を build-data で自動埋め込み。

## 推奨着手順

~~現在地マーカー → 店名検索 → 行った/行きたいマーク~~（すべて対応済み）

## 実装メモ

- `ShopMarker` の `position` は `useMemo` で参照を安定させること。毎レンダー新しい配列を渡すと react-leaflet が `setLatLng` を呼び、markercluster がマーカーを付け外しして開いているポップアップが閉じる。
